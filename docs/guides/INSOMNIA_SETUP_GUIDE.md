# 🔧 Insomnia: Настройка переменных окружения

## 📋 Пошаговая инструкция

### Шаг 1: Открыть Environment Manager

После импорта коллекции:
1. В левом верхнем углу Insomnia найдите выпадающий список **"No Environment"** или **"Base Environment"**
2. Кликните на него
3. Выберите **"Manage Environments"** (или кликните на иконку шестеренки ⚙️)

---

### Шаг 2: Настроить Base Environment

Вы увидите окно с JSON переменных. Должно быть так:

```json
{
  "base_url": "http://localhost:3000",
  "user_token": "",
  "moderator_token": "",
  "new_beam_id": "",
  "draft_id": "",
  "formed_id": "",
  "new_user_token": ""
}
```

**ВАЖНО:** Пока оставьте пустые строки - заполним их после выполнения запросов.

---

## 🔑 Когда и как обновлять токены

### Запрос #17: POST Sign In (User)

**1. Выполните запрос:**
```
POST http://localhost:3000/api/auth/sign_in
Body:
{
  "email": "user@demo.com",
  "password": "password123"
}
```

**2. В ответе получите:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjozOSwiZXhwIjoxNzY0NTI5NTUwfQ.h61r9z0Rud3pyUVrYoNrO88wr-xf6Vq8Z8iJN1FFQV4",
  "user": {
    "id": 39,
    "email": "user@demo.com",
    "moderator": false
  }
}
```

**3. Скопируйте значение `token`:**
- Выделите весь токен (начинается с `eyJ...`)
- Ctrl+C для копирования

**4. Откройте Environment Manager:**
- Кликните на выпадающий список вверху
- Manage Environments

**5. Вставьте токен в `user_token`:**
```json
{
  "base_url": "http://localhost:3000",
  "user_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjozOSwiZXhwIjoxNzY0NTI5NTUwfQ.h61r9z0Rud3pyUVrYoNrO88wr-xf6Vq8Z8iJN1FFQV4",
  "moderator_token": "",
  ...
}
```

**6. Нажмите "Done" для сохранения**

---

### Запрос #18: POST Sign In (Moderator)

**1. Выполните запрос:**
```
POST http://localhost:3000/api/auth/sign_in
Body:
{
  "email": "moderator@demo.com",
  "password": "password123"
}
```

**2. В ответе получите:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjo0MCwiZXhwIjoxNzY0NTI5NTUwfQ.74Ud9dJZ1pPFzydT-yMWA-o5eqH0nOYmNlKiVtZJLl0",
  "user": {
    "id": 40,
    "email": "moderator@demo.com",
    "moderator": true
  }
}
```

**3. Скопируйте токен и вставьте в `moderator_token`:**
```json
{
  "base_url": "http://localhost:3000",
  "user_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjozOSwiZXhwIjoxNzY0NTI5NTUwfQ.h61r9z0Rud3pyUVrYoNrO88wr-xf6Vq8Z8iJN1FFQV4",
  "moderator_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjo0MCwiZXhwIjoxNzY0NTI5NTUwfQ.74Ud9dJZ1pPFzydT-yMWA-o5eqH0nOYmNlKiVtZJLl0",
  ...
}
```

---

### Запрос #5: POST Create Beam

**1. Выполните запрос** (от имени модератора)

**2. В ответе получите:**
```json
{
  "id": 45,
  "name": "Test Beam Demo",
  "material": "steel",
  ...
}
```

**3. Скопируйте `id` и вставьте в `new_beam_id`:**
```json
{
  ...
  "new_beam_id": "45",
  ...
}
```

**ВАЖНО:** ID как строка (в кавычках) или число (без кавычек) - оба варианта работают.

---

### Запрос #9: GET Cart Badge

**1. Выполните запрос** (после добавления услуг)

**2. В ответе получите:**
```json
{
  "beam_deflection_id": 53,
  "items_count": 5
}
```

**3. Скопируйте `beam_deflection_id` в `draft_id`:**
```json
{
  ...
  "draft_id": "53",
  ...
}
```

---

### Запрос #16: POST Sign Up (новый пользователь)

**1. В ответе получите:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9...",
  "user": {
    "id": 41,
    "email": "newuser@test.com",
    "moderator": false
  }
}
```

**2. Скопируйте токен в `new_user_token`** (опционально, для дальнейшего использования)

---

## 📊 Итоговые переменные окружения

После выполнения всех запросов у вас должно получиться:

```json
{
  "base_url": "http://localhost:3000",
  "user_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjozOSwiZXhwIjoxNzY0NTI5NTUwfQ.h61r9z0Rud3pyUVrYoNrO88wr-xf6Vq8Z8iJN1FFQV4",
  "moderator_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjo0MCwiZXhwIjoxNzY0NTI5NTUwfQ.74Ud9dJZ1pPFzydT-yMWA-o5eqH0nOYmNlKiVtZJLl0",
  "new_beam_id": "45",
  "draft_id": "53",
  "formed_id": "52",
  "new_user_token": "eyJhbGciOiJIUzI1NiJ9..."
}
```

---

## 🎯 Как используются переменные в запросах

### Пример 1: Authorization Header
```
Authorization: Bearer {{ _.user_token }}
```
Insomnia автоматически подставит значение из `user_token`.

### Пример 2: URL с параметром
```
GET http://{{ _.base_url }}/api/beams/{{ _.new_beam_id }}
```
Превратится в:
```
GET http://localhost:3000/api/beams/45
```

### Пример 3: Body с переменной
```json
{
  "beam_id": {{ _.new_beam_id }}
}
```

---

## 🔄 Быстрый способ копирования

### Через контекстное меню:
1. В ответе кликните правой кнопкой на значение (например, на токен)
2. Выберите **"Copy Value"**
3. Откройте Environment Manager
4. Вставьте (Ctrl+V) в нужную переменную

### Через поиск:
1. В ответе используйте поиск (Ctrl+F)
2. Найдите нужное поле (`token`, `id`)
3. Скопируйте значение
4. Вставьте в Environment

---

## ⚠️ Важные моменты

### 1. Порядок выполнения
**ОБЯЗАТЕЛЬНО сначала выполните #17 и #18** (sign in), чтобы получить токены. Без токенов остальные запросы вернут 401 Unauthorized.

### 2. Срок действия токенов
Токены действительны **24 часа** (см. PROJECT_SNAPSHOT.md - там токены до 2025-12-29).

Если токен истек:
- Выполните снова запрос #17 или #18
- Обновите переменную окружения новым токеном

### 3. После sign_out
После запроса #21 (Sign Out) токен добавляется в blacklist.

**Нужно снова:**
1. Выполнить #17 или #18
2. Получить новый токен
3. Обновить переменную окружения

### 4. Docker перезапуск
Если вы перезапустили Docker (и пересоздали базу), ID объектов изменятся.

**Нужно обновить:**
- `new_beam_id` - после создания новой балки
- `draft_id` - после получения cart_badge
- Возможно, `user_token` и `moderator_token` (если пользователи пересозданы)

---

## 🚀 Быстрый старт (TL;DR)

### Шаг 1: Импортируйте коллекцию
```
Insomnia → Import → Insomnia_Collection_DEMO.json
```

### Шаг 2: Выполните sign in запросы
1. **Запрос #17** → Скопируйте `token` → Вставьте в `user_token`
2. **Запрос #18** → Скопируйте `token` → Вставьте в `moderator_token`

### Шаг 3: Выполняйте остальные запросы по порядку
При необходимости обновляйте:
- `new_beam_id` после запроса #5
- `draft_id` после запроса #9

### Шаг 4: Проверьте результаты
Через Rails console или следующие запросы в коллекции.

---

## 💡 Альтернатива: Pre-filled токены

Если вы уже создали демо-пользователей через `prepare_demo.rb`, можете сразу использовать готовые токены из PROJECT_SNAPSHOT.md:

```json
{
  "base_url": "http://localhost:3000",
  "user_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjozOSwiZXhwIjoxNzY0NTI5NTUwfQ.h61r9z0Rud3pyUVrYoNrO88wr-xf6Vq8Z8iJN1FFQV4",
  "moderator_token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjo0MCwiZXhwIjoxNzY0NTI5NTUwfQ.74Ud9dJZ1pPFzydT-yMWA-o5eqH0nOYmNlKiVtZJLl0",
  "new_beam_id": "39",
  "draft_id": "51",
  "formed_id": "52"
}
```

**Но:** Эти токены действительны только до **2025-12-29**!

---

## 🎬 Видео-инструкция (текстовая версия)

### Минута 1: Импорт
- Открываем Insomnia
- Import/Export → Import Data → From File
- Выбираем Insomnia_Collection_DEMO.json
- Видим 21 запрос + EXTRA

### Минута 2: Environment
- Кликаем на выпадающий список вверху
- Manage Environments
- Видим пустые переменные
- Пока не трогаем, закрываем

### Минута 3: Sign In User
- Выбираем запрос "17. POST Sign In (User)"
- Нажимаем Send
- В ответе видим `"token": "eyJ..."`
- Копируем весь токен (Ctrl+C)

### Минута 4: Обновляем переменную
- Открываем Manage Environments
- Находим `"user_token": ""`
- Вставляем токен между кавычек
- Нажимаем Done

### Минута 5: Sign In Moderator
- Выбираем запрос "18. POST Sign In (Moderator)"
- Send → Копируем токен → Вставляем в `moderator_token`

### Минута 6: Готово!
- Теперь все остальные запросы будут автоматически использовать токены
- Выполняем по порядку от 01 до 21

---

**Готово!** Теперь вы знаете, как обновлять переменные окружения в Insomnia. 🎉
